name: Trigger Copilot Index (Dev)

on:
  workflow_dispatch:

jobs:
  invoke-copilot-index-dev:
    name: Call Copilot index endpoint (Dev)
    runs-on: ubuntu-latest
    env:
      APOLO_AUTH_TOKEN: ${{ secrets.APOLO_AUTH_TOKEN_DEV }}
      COPILOT_INDEX_ENDPOINT: https://api.dev.apolo.us/apis/copilot/index

    steps:
      - name: Verify Apolo auth token secret is present
        run: |
          if [ -z "$APOLO_AUTH_TOKEN" ]; then
            echo "APOLO_AUTH_TOKEN_DEV secret is not configured."
            exit 1
          fi

      - name: Trigger documentation reindex via Copilot
        run: |
          curl --fail-with-body --show-error --silent \
            -X POST "$COPILOT_INDEX_ENDPOINT" \
            -H "Authorization: Bearer $APOLO_AUTH_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"pull_latest": true, "run_test_query": false}'
